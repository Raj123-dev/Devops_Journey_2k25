apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  labels:
    name: prometheus-config
    app: prometheus
    component: server
    chart: prometheus
    release: prometheus
    heritage: Helm
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'flask-app'
        static_configs:
          - targets: ['flask-app:5000']
        metrics_path: /metrics
