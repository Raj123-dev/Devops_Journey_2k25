apiVersion: v1
kind: Service
metadata:
  name: registration-form-service
  labels:
    app: registration-form-app
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: registration-form-app